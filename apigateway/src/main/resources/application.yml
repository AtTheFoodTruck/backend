server:
  port: 8005

eureka:
#  instance:
#    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka

# @author:jaemin - route, filter 설정
spring:
  application:
    name: apigateway-service
  cloud:
    gateway:
#      default-filters:
#        filters:
#          - name: LoggingFilter
#            args:
#              baseMessage: Start Logging Filter.
#              preLogger: true
#              postLogger: true
      routes:
        # 로그인, 회원가입 요청 route config
        - id: user-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/user-service/users/login,/user-service/users/join
            - Method=POST
          filters:
            - RemoveRequestHeader=Cookie
            - RewritePath=/user-service/(?<segment>.*), /$\{segment}
        # user-service의 모든 요청 filter처리
        - id: user-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/user-service/users/**
            - Method=GET,PATCH,DELETE
          filters:
            - RemoveRequestHeader=Cookie
            - RewritePath=/user-service/(?<segment>.*), /$\{segment}
            - name: JwtAuthentication
              args:
                role: ROLE_USER
        # 회원정보수정 [username, password]
        - id: user-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/user-service/name,/user-service/password
            - Method=PATCH
          filters:
            - RemoveRequestHeader=Cookie
            - RewritePath=/user-service/(?<segment>.*), /$\{segment}
            - name: JwtAuthentication
              args:
                role: ROLE_USER,ROLE_MANAGER
        # 회원정보 삭제
        - id: user-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/user-service/mypage
            - Method=DELETE
          filters:
            - RemoveRequestHeader=Cookie
            - RewritePath=/user-service/(?<segment>.*), /$\{segment}
            - name: JwtAuthentication
              args:
                role: ROLE_USER,ROLE_MANAGER

  # redis 설정
#  redis:
#    lettuce:
#      pool:
#        max-active: 10
#        max-idle: 10
#        min-idle: 2
#    port: 6379  # default: 6379
#    host: localhost  # default: localhost

# gw process 추적
logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    reactor.netty.http.client: DEBUG
